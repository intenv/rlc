tickTime=2000
dataDir=/var/lib/zookeeper/
clientPort=2181
initLimit=5
syncLimit=2
{% for host in groups['confluent'] %}
server.{{ hostvars[host].ZK_serverid }}={{ host }}:{{ ZK_leaderport }}:{{ ZK_electionport }}
{% endfor %}
autopurge.snapRetainCount=3
autopurge.purgeInterval=24
